# Welsh ASR (Wav2Vec2, HuBERT, Whisper)

## 🌟 Обзор проекта

Этот репозиторий содержит скрипты для **обучения трех популярных моделей автоматического распознавания речи (ASR)** — Wav2Vec2, HuBERT и Whisper — на **валлийском языке (Welsh)**.

***

## 📁 Структура проекта

Проект организован для удобной работы с обучающими и тестовыми данными. Все скрипты используют **динамическую адресацию**, базирующуюся на текущей рабочей директории.

```bash
Welsh-ASR/
├── train/
│   ├── train.tsv           # Файл с метками (транскрипциями)
│   └── welsh_wav/          # Папка с аудиофайлами .wav (для обучения)
├── test/
│   ├── transcripts_output_wav.csv  # Файл с метками для оценки
│   └── welsh_wav_test/     # Папка с аудиофайлами .wav (для оценки)
├── wav2vec2_fine_tuning.py       # Скрипт для обучения Wav2Vec2-XLSR
├── HuBERT_fine_tuning.py         # Скрипт для обучения HuBERT
├── whisper_fine_tuning.py        # Скрипт для обучения Whisper-Small
├── HuBERT_test.py          # Скрипт для оценки HuBERT
├── whisper_test.py        # Скрипт для обучения Whisper-Small
├── wav2vec2_test.py          # Скрипт для оценки HuBERT
└── README.md
```

## ⚙️ Требования и установка

### 1. Версии компонентов

Для воспроизведения результатов и стабильной работы на GPU использовалось следующее окружение:

| Компонент  | Версия |  Назначение    |
|-------|-----|-------|
| Python | 3.12  | Основной язык проекта. |
| PyTorch (Torch)   | 2.7.0  | Глубокое обучение и тензорные операции.   |
| CUDA   | 12.8  | Ускорение обучения на GPU.   |
| FFmpeg   | 2025-11-02-git-f5eb11a71d-full_build  | Необходим для корректной обработки аудио библиотекой librosa.   |

### 2. Установка библиотек

Установите все необходимые библиотеки с помощью pip:

```bash pip install torch pandas librosa evaluate transformers datasets accelerate```

## 🚀 Использование (Пример: Fine-Tuning Whisper)

Скрипты используют библиотеку datasets от Hugging Face и настроены на динамическую загрузку данных (Lazy Loading). Обучение автоматически сохраняет модель с лучшим показателем WER.

### 1. Запуск обучения

Для запуска переобучения модели Whisper-Small:

```bash python whisper_fine_tuning.py```

- Модель: openai/whisper-small

- Язык: Валлийский (welsh)

- Параметры: Все гиперпараметры (batch size, epochs, learning rate) задаются в секции КОНФИГУРАЦИЯ каждого скрипта.

### 2. Запуск оценки

После обучения вы можете оценить готовую модель на тестовом наборе, используя ее сохраненный путь:

1. Удалите: VOCAB_FILE_PATH (только для wav2vec2_test.py).

2. Замените: MODEL_ID на путь к папке.

3. Запустите: ```bash python whisper_test.py```

## 📊 Метрика оценки

Для оценки производительности моделей ASR используется стандартная метрика Word Error Rate (WER).

WER измеряет количество ошибок, необходимое для преобразования распознанного текста в эталонную транскрипцию.

$$\text{WER} = \frac{S + D + I}{N}$$

Где:
- $S$ — количество замен (Substitutions),
- $D$ — количество удалений (Deletions),
- $I$ — количество вставок (Insertions),
- $N$ — общее количество слов в эталонной транскрипции.

Чем ниже значение WER (ближе к 0), тем выше точность модели.

## 💾 Параметры обучения и сохранение

Для управления процессом обучения и эффективного использования дискового пространства используются следующие параметры из класса TrainingArguments (Hugging Face Trainer):

| Параметр  | Значение |  Назначение    |
|-------|-----|-------|
| load_best_model_at_end | True  | Загружает модель с лучшими метриками (наименьшим WER) в конце обучения, игнорируя последний, возможно, худший чекпойнт. |
| metric_for_best_model   | "wer"  | Указывает, что для определения лучшей модели нужно отслеживать метрику WER.   |
| greater_is_better   | False  | Определяет, что для метрики WER меньшее значение лучше.   |
| save_steps   | 500  | Сохраняет чекпойнт модели через каждые 500 шагов обучения.   |
| save_total_limit   | 2  | Ограничивает количество сохраненных чекпойнтов на диске до 2, автоматически удаляя самые старые чекпойнты.   |

### Финальное сохранение

В конце обучения используется метод trainer.save_model(OUTPUT_DIR), который сохраняет финальную модель в папку OUTPUT_DIR. После завершения обучения сохраняются не только сама модель, но и процессор (Feature Extractor / Tokenizer), необходимый для корректной обработки входных данных.
